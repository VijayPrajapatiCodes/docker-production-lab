
---

# `10_ENTRYPOINT.md`

```markdown
# 🐳 ENTRYPOINT Instruction

## 1. ENTRYPOINT Kya Hai?

`ENTRYPOINT` container ka main executable/application define karta hai.

Example:

```dockerfile
ENTRYPOINT ["java", "-jar", "app.jar"]