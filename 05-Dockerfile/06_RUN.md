
---

# `06_RUN.md`

```markdown
# 🐳 RUN Instruction

## 1. RUN Kya Hai?

`RUN` Docker image build hone ke time command execute karta hai.

Example:

```dockerfile
FROM ubuntu:24.04

RUN apt-get update