
---

# `09_CMD.md`

```markdown
# 🐳 CMD Instruction

## 1. CMD Kya Hai?

`CMD` container start hone par default command ya default arguments define karta hai.

Example:

```dockerfile
CMD ["java", "-jar", "app.jar"]