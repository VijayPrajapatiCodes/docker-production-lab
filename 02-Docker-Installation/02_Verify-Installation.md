# Verify Docker Installation

After installing Docker Desktop, we need to verify that Docker CLI can communicate with the Docker Engine successfully.

---

# 1. Check Docker Version

Use the following command:

```bash
docker --version