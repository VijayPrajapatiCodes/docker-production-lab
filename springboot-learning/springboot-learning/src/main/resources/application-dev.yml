server:
  port: 8081

app:
  environment: Development

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/backend_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: vijayprajapati24645@gmail.com
    password: qodj xxvu pbqa ewiz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  main:
    banner-mode: off

management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    health:
      show-details: always

  prometheus:
    metrics:
      export:
        enabled: true

